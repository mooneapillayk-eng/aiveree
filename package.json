{
  "name": "aiveree",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest run",
    "engine": "node engine/cli.mjs",
    "engine:verbose": "node engine/cli.mjs --verbose",
    "engine:reset": "node engine/cli.mjs --reset --verbose",
    "engine:live": "node engine/cli.mjs --live --verbose"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.0",
    "vitest": "^1.6.0"
  }
}
